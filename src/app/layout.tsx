import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { AppShell } from "@/components/layout/AppShell";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ASTRASENSE – AI Powered Environmental Risk Detection",
  description:
    "AI-powered environmental intelligence dashboard for satellite-derived hazard risk detection (floods, droughts, vegetation stress, land degradation).",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-app text-app-foreground`}
      >
        <AppShell>{children}</AppShell>
      </body>
    </html>
  );
}
